# Changelog fragments

Every pull request with a user-visible change must add one Markdown fragment
named `<issue-or-pr>.<added|changed|deprecated|removed|fixed|security>.md`.
Fragments are folded into `CHANGELOG.md` during release preparation.

